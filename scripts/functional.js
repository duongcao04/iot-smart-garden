// https://sweetalert2.github.io/
const henGioMayBom = (event) => {
    event.preventDefault()
    Swal.fire('Hẹn giờ máy bơm thành công')
}

const henGioQuat = (event) => {
    event.preventDefault()
    Swal.fire('Hẹn giờ quạt thành công')
}

const henGioDen = (event) => {
    event.preventDefault()
    Swal.fire('Hẹn giờ đèn thành công')
}
